# Asset Index

| File | Purpose |
|------|---------|
| `Cantrip.ico` | Cantrip C-and-spark executable/installer icon, with 16/24/32/48/64/128/256px frames |

Regenerate with `make artwork` from the repository root on macOS. The shared
vector source is `Scripts/generate-artwork.swift`; see the root README's
App artwork section. Windows builds consume the checked-in ICO directly.
